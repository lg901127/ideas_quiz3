<h1>Edit idea</h1>
<%= @idea.errors.full_messages.join(", ") %>
<%= form_for @idea do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>
  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <%= f.submit %>
<% end %>
